/**
 * M05 협력 네트워크 — Sprint 0 placeholder.
 *
 * CI 의 `playwright test tests/e2e/m05-network` 가 "no tests found" 로 실패하지
 * 않도록 1개 trivial test 를 둔다. Sprint 5 진입 시 본 파일을 삭제하고
 * 실제 spec 으로 대체한다.
 *
 * Plan: 9 기능
 */
import { test } from '@playwright/test';

test.describe('M05 협력 네트워크 (placeholder)', () => {
  test('Sprint 5 에서 구현 예정', () => {
    // intentionally empty — Sprint 5 진입 시 _placeholder.spec.ts 삭제
  });
});

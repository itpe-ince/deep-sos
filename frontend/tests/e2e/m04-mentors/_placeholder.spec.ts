/**
 * M04 멘토·학생팀 매칭 — Sprint 0 placeholder.
 *
 * CI 의 `playwright test tests/e2e/m04-mentors` 가 "no tests found" 로 실패하지
 * 않도록 1개 trivial test 를 둔다. Sprint 4 진입 시 본 파일을 삭제하고
 * 실제 spec 으로 대체한다.
 *
 * Plan: 9 기능
 */
import { test } from '@playwright/test';

test.describe('M04 멘토·학생팀 매칭 (placeholder)', () => {
  test('Sprint 4 에서 구현 예정', () => {
    // intentionally empty — Sprint 4 진입 시 _placeholder.spec.ts 삭제
  });
});
